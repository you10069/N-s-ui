# S-UI 精简配置版

本分支以 s-ui 1.1.0 为基础，参考 x-ui 1.11.3 与 3x-ui 3.4.2 的配置编辑和用户流量管理思路。

## 页面

- 主页：保留原仪表和系统信息，新增磁盘与 SWAP 仪表。
- 配置文件：直接读取和编辑 `/usr/local/s-ui/bin/config.json`，支持 JSON 注释、复制、粘贴、检查、保存和手动重载 sing-box。
- 用户管理：自动读取 `inbounds[].users[]`，显示上传、下载、总流量、倍率后流量、到期时间和月重置日。
- 管理员、设置：保留。

原“入站、出站、路由、TLS、基础信息”表单页面已从路由和菜单中移除。配置文件是 sing-box 配置的唯一来源。

## 配置保存行为

- “检查配置”调用实际安装的 `sing-box check -c <临时文件>`。
- “保存”先检查配置，再原样写入 `config.json`，注释、空行和字段顺序都会保留。
- 保存不会自动重载 sing-box。
- “重载 sing-box”是独立手动操作。

## 用户流量统计要求

用户名称优先从 `name`、`username`、`email` 字段读取；缺少这些字段时使用 `uuid` 作为后备标识。

要获得用户流量，sing-box 配置必须启用 V2Ray API 统计，并将用户名称加入 `stats.users`：

```jsonc
{
  "experimental": {
    "v2ray_api": {
      "listen": "127.0.0.1:8080",
      "stats": {
        "enabled": true,
        "users": [
          "user-a",
          "user-b"
        ]
      }
    }
  }
}
```

面板不会自动重写 `stats.users`，以免破坏手工维护的配置和注释。

## 倍率和重置

- 原始总流量：`上传 + 下载`。
- 倍率后流量：`(上传 + 下载) × 倍率`。
- 月重置日为 1–31；短月份中超出当月天数的值按当月最后一天处理。
- 面板离线错过重置日时，会在当月下一次定时检查时补重置。
- 手动重置会清零上传、下载，并记录最后重置时间。
- 到期或达到流量上限时，用户会从运行配置中自动移除，但不会删除 `config.json` 中的原始用户。

## 构建

```bash
./build.sh
```

前端要求 Node.js/npm，后端要求 Go 1.23.2 或兼容版本。

## 自动限制与运行配置

本版本将人工维护配置与 sing-box 实际运行配置分开：

- `/usr/local/s-ui/bin/config.json`：编辑中的源配置，保留完整用户、注释、空行和手工排版；保存后需手动点击重载。
- `/usr/local/s-ui/bin/config.active.json`：最近一次重载生效的配置快照，不建议人工编辑。
- `/usr/local/s-ui/bin/config.runtime.json`：在生效快照基础上过滤受限用户后生成的运行配置，不建议人工编辑。

用户出现以下任一情况时，会从运行配置中移除：

- 手动关闭“允许该用户连接”；
- 当前时间达到到期时间；
- `(上传 + 下载) × 倍率` 达到总流量上限。

运行配置变化后，s-ui 会完整重启 sing-box，以关闭已经存在的连接。源配置中的用户不会被删除。延长到期时间、增加额度、手动重置或每月自动重置后，用户会自动恢复。

如果某个入站原本配置了用户认证，而过滤后一个用户也不剩，s-ui 会从运行配置中移除整个入站，避免空 `users` 使 SOCKS/HTTP 入站意外变成免认证代理。

为保证流量统计和限制准确，所有用户名称应全局唯一，并在 `experimental.v2ray_api.stats.users` 中使用相同名称。


## sing-box 版本

Docker 与 Release 工作流默认构建上游 sing-box `v1.12.25`，并启用 WireGuard Endpoint、V2Ray API 与 Clash API 等构建标签。若要使用你定制的 `direct -> detour`、`clear_resolved` 版本，请用定制源码编译出的 `sing-box` 替换发布包中的 `s-ui/bin/sing-box`。

## 双栏配置查看

配置页面左侧直接编辑 `config.json`，右侧只读展示当前实际交给 sing-box 的 `config.runtime.json`。运行配置会包含用户到期、流量限额和手动禁用后的过滤结果，可独立刷新和复制；保存源配置不会改变右侧内容，只有手动重载或自动限额状态变化后才会更新。
